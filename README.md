# proto.vroy.ca

This is a ramaze prototype used for vroy.ca subdomain apps. It's easy to use: `cp -r proto.vroy.ca new-app.vroy.ca` and replace the instances of proto to new-app in the new folder structure, hint: `grep -r -i 'proto' * | grep 'vroy.ca'`
